Run npm install in both folders(storeApp, express) to install dependencies.
Check .env to setup environment variables
To run the app, run 2 terminals, one is storeApp than run ng serve, other is express than run nodemon server.js
